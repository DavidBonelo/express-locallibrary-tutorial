extends layout
block content
    h1= title

    div(style='margin-left:20px;margin-top:20px')

        h4 Books

        dl
        each book in book_list
            dt
                a(href=book.url) #{book.title}
            dd #{book.summary}
        else 
            li There are no books of this genre.